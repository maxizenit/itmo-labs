spring:
  datasource:
    url: jdbc:h2:mem:db
    username: sa
    password: password
  h2:
    console:
      enabled: true
  grpc:
    server:
      port: 9091

server:
  port: 8081

eureka:
  instance:
    prefer-ip-address: true

logging:
  level:
    root: debug